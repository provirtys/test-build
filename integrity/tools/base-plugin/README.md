# base-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build base-plugin` to build the library.

## Running unit tests

Run `nx test base-plugin` to execute the unit tests via [Jest](https://jestjs.io).
